# Android Import E2E Captures

1. `01-import-confirmation.png`: the app receives and reviews a transfer-v1 HTTPS import intent.
2. `02-imported-list.png`: confirmation creates an independent local list and navigates to it.

Captured on `emulator-5554` from the debug APK. The intent is explicitly targeted at `app.cuckoocue`; verified browser-to-app routing still depends on the pending canonical-domain DNS and certificate.
